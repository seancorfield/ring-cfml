<cfoutput>
  <h1>Hello</h1>
  <p>Default middleware, handler.</p>
  <p>The only difference from the <tt>1inline</tt> example is that <tt>Application.cfc</tt>
    delegates the <tt>onRequest*()</tt> methods to Ring, instead of just extending it.</p>
  <p><a href="two.cfm">Page 2</a></p>
</cfoutput>
